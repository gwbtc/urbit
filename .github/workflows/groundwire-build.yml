name: Build Groundwire
on:
  push:
    branches:
      - gw/next/kelvin/408
  pull_request:
    branches:
      - gw/next/kelvin/408
  workflow_dispatch:

env:
  URBIT_BRANCH: gw/next/kelvin/408
  GROUNDWIRE_BRANCH: hd/consolidate-urb
  SPV_WALLET_BRANCH: develop
  URCRYPT_BRANCH: gw/cmp-pub
  VERE_BRANCH: gw/next/kelvin/408
  COMET_MINER_BRANCH: tinnus-feed
  URBIT_MCP_BRANCH: main
  URSOCKETS_BRANCH: develop
  TLONDESKS_BRANCH: polwex/tlondesks
  BOOTING_BRANCH: main

jobs:
  # =============================================================
  # Build the brass pill on a fake ~zod
  #
  # Flow:
  #   1. Boot fake ~zod with custom vere + kelvin 408 arvo (-A flag)
  #   2. For each desk: create from %base, mount, clear, populate, commit
  #   3. Write each desk's agent list to Clay via %info (filesystem commits are unreliable)
  #   4. Build brass pill via fyrd, wait for it to appear on filesystem
  #
  # Key gotchas:
  #   - desk-create.sh copies ALL of %base (including desk.bill with
  #     %acme etc). Must clear and replace before pill build.
  #   - Filesystem commits need mark files (mar/bill.hoon etc) to work.
  #     desk-populate.sh preserves mar/ when clearing.
  #   - Even with marks, filesystem commits can be unreliable. The %info
  #     step writes desk.bill directly to Clay as a safety net.
  # =============================================================
  build-pill:
    name: Build pill
    needs: build-vere
    runs-on: ubuntu-latest
    env:
      CONN_SOCK: ./zod/.urb/conn.sock
    steps:
      - name: Clean up leftover piers
        run: sudo rm -rf ./zod || true

      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Download custom vere (kelvin 408 compatible)
        uses: actions/download-artifact@v4
        with:
          name: gw-vere-linux-x86_64
          path: ./vere-bin

      - name: Install custom vere
        run: |
          chmod +x ./vere-bin/gw-vere
          sudo mv ./vere-bin/gw-vere /usr/local/bin/urbit
          urbit --version || true

      - name: Install system dependencies
        run: |
          sudo apt-get update -y
          sudo apt-get install -y netcat-openbsd

      - name: Install peru
        run: |
          sudo apt-get install -y pipx
          pipx ensurepath
          pipx install peru
          echo "$HOME/.local/bin" >> $GITHUB_PATH

      # -- Checkout all desk source repos --

      - name: Checkout gwbtc/urbit
        uses: actions/checkout@v4
        with:
          repository: gwbtc/urbit
          ref: ${{ env.URBIT_BRANCH }}
          path: gwbtc-urbit
          token: ${{ secrets.GH_PAT }}

      - name: Checkout gwbtc/groundwire
        uses: actions/checkout@v4
        with:
          repository: gwbtc/groundwire
          ref: ${{ env.GROUNDWIRE_BRANCH }}
          path: gwbtc-groundwire
          token: ${{ secrets.GH_PAT }}

      - name: Checkout gwbtc/spv-wallet
        uses: actions/checkout@v4
        with:
          repository: gwbtc/spv-wallet
          ref: ${{ env.SPV_WALLET_BRANCH }}
          path: gwbtc-spv-wallet
          token: ${{ secrets.GH_PAT }}

      - name: Checkout gwbtc/urbit-mcp
        uses: actions/checkout@v4
        with:
          repository: gwbtc/urbit-mcp
          ref: ${{ env.URBIT_MCP_BRANCH }}
          path: gwbtc-urbit-mcp
          token: ${{ secrets.GH_PAT }}

      - name: Checkout gwbtc/ursockets (nostrill desk)
        uses: actions/checkout@v4
        with:
          repository: gwbtc/ursockets
          ref: ${{ env.URSOCKETS_BRANCH }}
          path: gwbtc-ursockets
          token: ${{ secrets.GH_PAT }}

      - name: Checkout gwbtc/ursockets tlondesks (landscape + groups)
        uses: actions/checkout@v4
        with:
          repository: gwbtc/ursockets
          ref: ${{ env.TLONDESKS_BRANCH }}
          path: gwbtc-tlondesks
          token: ${{ secrets.GH_PAT }}

#      - name: Checkout tloncorp/tlon-apps (groups desk)
#        uses: actions/checkout@v4
#        with:
#          repository: tloncorp/tlon-apps
#          path: tlon-apps
#          token: ${{ secrets.GH_PAT }}

#      - name: Checkout tloncorp/landscape (landscape desk)
#        uses: actions/checkout@v4
#        with:
#          repository: tloncorp/landscape
#          path: tloncorp-landscape
#          token: ${{ secrets.GH_PAT }}

      # -- Boot fake ~zod --

      - name: Boot fake ~zod with kelvin 408 arvo
        run: |
          # Stock vere rejects kelvin 408 (expects hoon 136, we have 135).
          # Custom vere from gwbtc/vere supports the new kelvins.
          # -A flag loads arvo directly, bypassing mount/commit for %base.
          urbit -d -F zod -A gwbtc-urbit/pkg/arvo/ -c zod

      - name: Wait for ~zod to be idle
        run: ./automation/wait-idle.sh "$CONN_SOCK"

      - name: Verify kelvin 408 boot
        run: |
          source ./automation/fyrd.sh
          read -r -d '' SCRY_FYRD << 'HOON' || true
          :*  0
              %fyrd  %base  %khan-eval  %noun  %ted-eval
              :_  :~  /sur/spider/hoon  /lib/strandio/hoon  ==
              '''
              =/  m  (strand ,vase)
              ^-  form:m
              ;<  our=@p   bind:m  get-our
              ;<  now=@da  bind:m  get-time
              =/  kel  .^(* %cx /(scot %p our)/base/(scot %da now)/sys/kelvin)
              (pure:m !>(;;(@t (crip (noah !>(kel))))))
              '''
          ==
          HOON
          RESULT=$(send_fyrd "$SCRY_FYRD")
          echo "Clay kelvin: $RESULT"

      - name: Mount %base desk
        run: ./automation/desk-mount.sh base ./zod "$CONN_SOCK"

      # -- Populate desks --
      # Each desk: create from %base → mount → clear (preserving marks)
      # → copy desk-specific content → ensure kelvin/bill → commit

      - name: Populate %gw-base desk
        run: |
          ./automation/desk-create.sh gw-base "$CONN_SOCK"
          ./automation/desk-mount.sh gw-base ./zod "$CONN_SOCK"
          ./automation/desk-populate.sh gw-base ./zod
          rsync -a --no-links --checksum --exclude='.git*' --exclude='.ignore' \
            gwbtc-urbit/pkg/arvo/ ./zod/gw-base/
          ./automation/desk-commit.sh gw-base ./zod "$CONN_SOCK"

      - name: Populate %groundwire desk
        run: |
          ./automation/desk-create.sh groundwire "$CONN_SOCK"
          ./automation/desk-mount.sh groundwire ./zod "$CONN_SOCK"
          ./automation/desk-populate.sh groundwire ./zod
          cd gwbtc-groundwire && chmod +x build.sh && bash build.sh -p ../zod/groundwire && cd ..
          ./automation/desk-commit.sh groundwire ./zod "$CONN_SOCK"

      - name: Populate %spv-wallet desk
        run: |
          ./automation/desk-create.sh spv-wallet "$CONN_SOCK"
          ./automation/desk-mount.sh spv-wallet ./zod "$CONN_SOCK"
          ./automation/desk-populate.sh spv-wallet ./zod
          rsync -a --no-links --checksum --exclude='.git*' --exclude='.ignore' \
            gwbtc-spv-wallet/desk/ ./zod/spv-wallet/
          ./automation/desk-commit.sh spv-wallet ./zod "$CONN_SOCK"

      - name: Populate %mcp desk
        run: |
          ./automation/desk-create.sh mcp "$CONN_SOCK"
          ./automation/desk-mount.sh mcp ./zod "$CONN_SOCK"
          ./automation/desk-populate.sh mcp ./zod
          cd gwbtc-urbit-mcp && chmod +x build.sh && bash build.sh -p ../zod/mcp && cd ..
          ./automation/desk-commit.sh mcp ./zod "$CONN_SOCK"

      - name: Populate %gwbtc-groups desk
        run: |
          ./automation/desk-create.sh gwbtc-groups "$CONN_SOCK"
          ./automation/desk-mount.sh gwbtc-groups ./zod "$CONN_SOCK"
          # Don't clear — groups needs base libs
          rsync -a --no-links --checksum --exclude='.git*' --exclude='.ignore' \
            gwbtc-tlondesks/tlon/groups/ ./zod/gwbtc-groups/
          ./automation/desk-commit.sh gwbtc-groups ./zod "$CONN_SOCK"

      - name: Populate %nostrill desk
        run: |
          ./automation/desk-create.sh nostrill "$CONN_SOCK"
          ./automation/desk-mount.sh nostrill ./zod "$CONN_SOCK"
          # Don't clear — nostrill needs base libs (default-agent, verb, etc.)
          rsync -a --no-links --checksum --exclude='.git*' --exclude='.ignore' \
            gwbtc-ursockets/app/ ./zod/nostrill/
          ./automation/desk-commit.sh nostrill ./zod "$CONN_SOCK"

      - name: Populate %gw-landscape desk
        run: |
          ./automation/desk-create.sh gw-landscape "$CONN_SOCK"
          ./automation/desk-mount.sh gw-landscape ./zod "$CONN_SOCK"
          # Don't clear — landscape needs base libs (default-agent, server, etc.)
          rsync -a --no-links --checksum --exclude='.git*' --exclude='.ignore' \
            gwbtc-tlondesks/tlon/landscape/ ./zod/gw-landscape/
          ./automation/desk-commit.sh gw-landscape ./zod "$CONN_SOCK"

#      - name: Populate %groups desk
#        run: |
#          ./automation/desk-create.sh groups "$CONN_SOCK"
#          ./automation/desk-mount.sh groups ./zod "$CONN_SOCK"
#          ./automation/desk-populate.sh groups ./zod
#          rsync -a --no-links --checksum --exclude='.git*' --exclude='.ignore' \
#            tlon-apps/desk/ ./zod/groups/
#          echo '[%zuse 408]' > ./zod/groups/sys.kelvin
#          ./automation/desk-commit.sh groups ./zod "$CONN_SOCK"
#          # groups has ~487 files — wait for Clay to finish processing
#          ./automation/wait-idle.sh "$CONN_SOCK"
#
#      - name: Populate %landscape desk
#        run: |
#          ./automation/desk-create.sh landscape "$CONN_SOCK"
#          ./automation/desk-mount.sh landscape ./zod "$CONN_SOCK"
#          ./automation/desk-populate.sh landscape ./zod
#          rsync -a --no-links --checksum --exclude='.git*' --exclude='.ignore' \
#            tloncorp-landscape/desk/ ./zod/landscape/
#          echo '[%zuse 408]' > ./zod/landscape/sys.kelvin
#          ./automation/desk-commit.sh landscape ./zod "$CONN_SOCK"
#          ./automation/wait-idle.sh "$CONN_SOCK"

      - name: Wait for Clay to settle
        run: ./automation/wait-idle.sh "$CONN_SOCK"

      - name: Build brass pill
        run: |
          source ./automation/fyrd.sh
          read -r -d '' BUILD_PILL_FYRD << 'HOON' || true
          :*  0
              %fyrd  %base  %khan-eval  %noun  %ted-eval
              :_  :~  /sur/spider/hoon  /lib/strandio/hoon  /lib/pill/hoon  ==
              '''
              =/  m  (strand ,vase)
              ^-  form:m
              ;<  our=@p   bind:m  get-our
              ;<  now=@da  bind:m  get-time
              =/  sys=path    /(scot %p our)/gw-base/(scot %da now)/sys
              =/  dez=(list [desk path])
                :~  [%groundwire /(scot %p our)/groundwire/(scot %da now)]
                [%spv-wallet /(scot %p our)/spv-wallet/(scot %da now)]
                [%mcp /(scot %p our)/mcp/(scot %da now)]
                [%groups /(scot %p our)/gwbtc-groups/(scot %da now)]
                [%nostrill /(scot %p our)/nostrill/(scot %da now)]
                [%landscape /(scot %p our)/gw-landscape/(scot %da now)]
                ==
              =/  =pill:pill  (brass:pill sys dez & | | ~)
              ;<  ~  bind:m
                %:  send-raw-card
                    %pass  /build
                    %arvo  %c
                    %info  %base  %&
                    :~  [/pill/pill %ins %pill !>(pill)]
                    ==
                ==
              (pure:m !>('Built pill!'))
              '''
          ==
          HOON
          fire_and_forget_fyrd "$BUILD_PILL_FYRD"
          echo "Pill build triggered."

      - name: Wait for pill to appear
        timeout-minutes: 45
        run: |
          for i in $(seq 1 600); do
            PILL_FILE=$(find ./zod/base/ -name "*.pill" -type f 2>/dev/null | head -1)
            if [ -n "$PILL_FILE" ]; then
              echo "Found pill at: $PILL_FILE"
              cp "$PILL_FILE" ./gw-base.pill
              ls -la ./gw-base.pill
              exit 0
            fi
            [ $((i % 60)) -eq 0 ] && echo "--- $i iterations, no pill yet ---"
            sleep 3
          done
          echo "ERROR: Pill never appeared."
          exit 1

      - name: Shutdown ~zod
        if: always()
        run: |
          kill "$(cat ./zod/.urb/pid 2>/dev/null)" 2>/dev/null || true
          sleep 3
          pkill -f "urbit" || true

      - name: Upload pill
        uses: actions/upload-artifact@v4
        with:
          name: gw-base-pill
          path: ./gw-base.pill

  # =============================================================
  # Build vere for each platform (pill build depends on this)
  # =============================================================
  build-vere:
    name: "Build vere: ${{ matrix.target }}"
    strategy:
      matrix:
        include:
          - target: linux-x86_64
            runner: ubuntu-latest
            zig_archive: zig-x86_64-linux-0.15.2.tar.xz
            zig_target: x86_64-linux-musl
          - target: linux-aarch64
            runner: ubuntu-24.04-arm
            zig_archive: zig-aarch64-linux-0.15.2.tar.xz
            zig_target: aarch64-linux-musl
          - target: macos-aarch64
            runner: macos-latest
            zig_archive: zig-aarch64-macos-0.15.2.tar.xz
            zig_target: aarch64-macos
    runs-on: ${{ matrix.runner }}
    steps:
      - name: Install Zig 0.15.2
        run: |
          curl -LO "https://ziglang.org/download/0.15.2/${{ matrix.zig_archive }}"
          tar xf "${{ matrix.zig_archive }}"
          ZIG_DIR="${{ matrix.zig_archive }}"
          ZIG_DIR="${ZIG_DIR%.tar.xz}"
          echo "$(pwd)/$ZIG_DIR" >> $GITHUB_PATH

      - name: Checkout gwbtc/urcrypt
        uses: actions/checkout@v4
        with:
          repository: gwbtc/urcrypt
          ref: ${{ env.URCRYPT_BRANCH }}
          path: gwbtc-urcrypt
          token: ${{ secrets.GH_PAT || github.token }}

      - name: Prepare urcrypt
        run: |
          rm -f gwbtc-urcrypt/build.zig gwbtc-urcrypt/build.zig.zon
          ln -sf $(pwd)/gwbtc-urcrypt $(pwd)/urcrypt

      - name: Checkout gwbtc/vere
        uses: actions/checkout@v4
        with:
          repository: gwbtc/vere
          ref: ${{ env.VERE_BRANCH }}
          path: gwbtc-vere
          token: ${{ secrets.GH_PAT || github.token }}

      - name: Build vere
        run: |
          cd gwbtc-vere
          for attempt in 1 2 3; do
            echo "Build attempt $attempt/3..."
            if zig build -Dtarget=${{ matrix.zig_target }}; then
              break
            elif [ $attempt -eq 3 ]; then
              # Last resort: try vendoring zlib if GitHub is rate-limiting
              HTTP_CODE=$(curl -sL -o /dev/null -w "%{http_code}" \
                "https://github.com/allyourcodebase/zlib/archive/61e7df7e996ec5a5f13a653db3c419adb340d6ef.tar.gz")
              if [ "$HTTP_CODE" != "200" ]; then
                mkdir -p ext/zlib-src
                curl -L -o /tmp/zlib.tar.gz \
                  "https://github.com/madler/zlib/releases/download/v1.3.1/zlib-1.3.1.tar.gz"
                tar xzf /tmp/zlib.tar.gz --strip-components=1 -C ext/zlib-src
                python3 -c "
          import re
          with open('build.zig.zon') as f: c = f.read()
          c = re.sub(r'(\.zlib\s*=\s*\.\{)\s*\.url\s*=\s*\"[^\"]+\",\s*\.hash\s*=\s*\"[^\"]+\",',
                     r'\1\n            .path = \"./ext/zlib-src\",', c)
          with open('build.zig.zon','w') as f: f.write(c)
          "
                zig build -Dtarget=${{ matrix.zig_target }} || exit 1
              else
                exit 1
              fi
            else
              sleep 15
            fi
          done
          VERE_BIN=$(find zig-out -type f -executable 2>/dev/null | head -1)
          [ -z "$VERE_BIN" ] && VERE_BIN=$(find zig-out -type f \( -name "urbit" -o -name "vere" \) 2>/dev/null | head -1)
          cp "$VERE_BIN" ../gw-vere
          cd ..
          chmod +x ./gw-vere
          ./gw-vere --version || true

      - name: Upload vere
        uses: actions/upload-artifact@v4
        with:
          name: gw-vere-${{ matrix.target }}
          path: ./gw-vere

  # =============================================================
  # Build comet-miner + gw-onboard for each platform
  # =============================================================
  build-extras:
    name: "Build extras: ${{ matrix.target }}"
    strategy:
      matrix:
        include:
          - target: linux-x86_64
            runner: ubuntu-latest
            zig_archive: zig-x86_64-linux-0.14.1.tar.xz
            zig_target: x86_64-linux-musl
          - target: linux-aarch64
            runner: ubuntu-24.04-arm
            zig_archive: zig-aarch64-linux-0.14.1.tar.xz
            zig_target: aarch64-linux-musl
          - target: macos-aarch64
            runner: macos-latest
            zig_archive: zig-aarch64-macos-0.14.1.tar.xz
            zig_target: aarch64-macos
    runs-on: ${{ matrix.runner }}
    steps:
      - name: Install Zig 0.14.1
        run: |
          curl -LO "https://ziglang.org/download/0.14.1/${{ matrix.zig_archive }}"
          tar xf "${{ matrix.zig_archive }}"
          ZIG_DIR="${{ matrix.zig_archive }}"
          ZIG_DIR="${ZIG_DIR%.tar.xz}"
          echo "$(pwd)/$ZIG_DIR" >> $GITHUB_PATH

      - name: Checkout gwbtc/urcrypt
        uses: actions/checkout@v4
        with:
          repository: gwbtc/urcrypt
          ref: ${{ env.URCRYPT_BRANCH }}
          path: gwbtc-urcrypt
          token: ${{ secrets.GH_PAT || github.token }}

      - name: Prepare urcrypt
        run: |
          rm -f gwbtc-urcrypt/build.zig gwbtc-urcrypt/build.zig.zon
          ln -sf $(pwd)/gwbtc-urcrypt $(pwd)/urcrypt

      - name: Checkout gwbtc/comet-miner
        uses: actions/checkout@v4
        with:
          repository: gwbtc/comet-miner
          ref: ${{ env.COMET_MINER_BRANCH }}
          path: gwbtc-comet-miner
          token: ${{ secrets.GH_PAT || github.token }}

      - name: Build comet-miner
        run: |
          cd gwbtc-comet-miner
          sed -i.bak 's/printf("tries: %llu\\n"/printf("tries: %" PRIu64 "\\n"/g' pkg/vere/comet_miner.c
          if ! grep -q '#include <inttypes.h>' pkg/vere/comet_miner.c; then
            sed -i.bak '1s/^/#include <inttypes.h>\n/' pkg/vere/comet_miner.c
          fi
          for attempt in 1 2 3; do
            echo "Build attempt $attempt/3..."
            zig build -Dtarget=${{ matrix.zig_target }} -Doptimize=ReleaseFast -Dprogram=comet_miner && break
            [ $attempt -eq 3 ] && exit 1
            sleep 15
          done
          MINER_BIN=$(find zig-out -type f -name "comet_miner" 2>/dev/null | head -1)
          [ -z "$MINER_BIN" ] && MINER_BIN=$(find zig-out -type f -executable 2>/dev/null | head -1)
          cp "$MINER_BIN" ../comet_miner
          cd ..
          chmod +x ./comet_miner

      - name: Upload comet-miner
        uses: actions/upload-artifact@v4
        with:
          name: comet-miner-${{ matrix.target }}
          path: ./comet_miner

      - name: Checkout gwbtc/booting
        uses: actions/checkout@v4
        with:
          repository: gwbtc/booting
          ref: ${{ env.BOOTING_BRANCH }}
          path: gwbtc-booting
          token: ${{ secrets.GH_PAT }}

      - name: Build gw-onboard
        run: |
          if [[ "$OSTYPE" == darwin* ]]; then
            sed -i '' 's|VERE_BIN = .*|VERE_BIN = "./gw-vere"|' gwbtc-booting/gw-onboard.py
            sed -i '' 's|COMET_MINER_BIN = .*|COMET_MINER_BIN = "./comet_miner"|' gwbtc-booting/gw-onboard.py
          else
            sed -i 's|VERE_BIN = .*|VERE_BIN = "./gw-vere"|' gwbtc-booting/gw-onboard.py
            sed -i 's|COMET_MINER_BIN = .*|COMET_MINER_BIN = "./comet_miner"|' gwbtc-booting/gw-onboard.py
          fi
          pip install pyinstaller requests embit
          pyinstaller --onefile --name gw-onboard gwbtc-booting/gw-onboard.py
          chmod +x dist/gw-onboard

      - name: Upload gw-onboard
        uses: actions/upload-artifact@v4
        with:
          name: gw-onboard-${{ matrix.target }}
          path: dist/gw-onboard


  # =============================================================
  # Validate the pill boots, then publish release
  # =============================================================
  validate-and-release:
    name: Validate and release
    needs: [build-pill, build-vere, build-extras]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: ./artifacts

      - name: Install system dependencies
        run: |
          sudo apt-get update -y
          sudo apt-get install -y netcat-openbsd

      - name: Boot test ship with pill
        env:
          VERE_BIN: ./gw-vere
          CONN_SOCK: ./zod2/.urb/conn.sock
        run: |
          cp ./artifacts/gw-vere-linux-x86_64/gw-vere ./gw-vere
          chmod +x ./gw-vere
          cp ./artifacts/gw-base-pill/gw-base.pill ./gw-base.pill
          ./gw-vere -d -F zod -B ./gw-base.pill -c zod2
          ./automation/wait-idle.sh "$CONN_SOCK" "$VERE_BIN"
          echo "Validation successful — ship booted with pill."

      - name: Shutdown test ship
        if: always()
        run: |
          kill "$(cat ./zod2/.urb/pid 2>/dev/null)" 2>/dev/null || true
          sleep 3
          pkill -f "gw-vere" || true

      - name: Install GitHub CLI
        run: |
          if ! command -v gh &>/dev/null; then
            curl -fsSL https://cli.github.com/packages/githubcli-archive-keyring.gpg | \
              sudo dd of=/usr/share/keyrings/githubcli-archive-keyring.gpg
            echo "deb [arch=$(dpkg --print-architecture) signed-by=/usr/share/keyrings/githubcli-archive-keyring.gpg] https://cli.github.com/packages stable main" | \
              sudo tee /etc/apt/sources.list.d/github-cli.list > /dev/null
            sudo apt-get update -y && sudo DEBIAN_FRONTEND=noninteractive apt-get install -y --no-install-recommends gh
          fi

      - name: Package and publish release
        run: |
          printf 'requests\nembit\n' > requirements.txt
          for target in linux-x86_64 linux-aarch64 macos-aarch64; do
            mkdir -p "pkg-${target}"
            cp ./gw-base.pill "pkg-${target}/"
            cp "./artifacts/gw-vere-${target}/gw-vere" "pkg-${target}/gw-vere"
            cp "./artifacts/comet-miner-${target}/comet_miner" "pkg-${target}/comet_miner"
            chmod +x "pkg-${target}/gw-vere" "pkg-${target}/comet_miner"
            cp "./artifacts/gw-onboard-${target}/gw-onboard" "pkg-${target}/gw-onboard"
            chmod +x "pkg-${target}/gw-onboard"
            cp requirements.txt "pkg-${target}/"
            tar czf "groundwire-${target}.tar.gz" -C "pkg-${target}" .
          done

          DATE_BASE=$(date -u +"%Y.%-m.%-d")
          TAG="groundwire-alpha-${DATE_BASE}"
          TITLE="groundwire-alpha ${DATE_BASE}"
          SUFFIX=2
          while gh release view "$TAG" --repo gwbtc/urbit &>/dev/null; do
            TAG="groundwire-alpha-${DATE_BASE}_${SUFFIX}"
            TITLE="groundwire-alpha ${DATE_BASE}_${SUFFIX}"
            SUFFIX=$((SUFFIX + 1))
          done

          gh release create "$TAG" \
            groundwire-linux-x86_64.tar.gz \
            groundwire-linux-aarch64.tar.gz \
            groundwire-macos-aarch64.tar.gz \
            --repo gwbtc/urbit \
            --title "$TITLE" \
            --notes "Automated build from \`${GITHUB_REF_NAME}\` (${GITHUB_SHA})

          Each tarball contains everything needed to onboard:
          - \`gw-vere\` — Urbit runtime
          - \`gw-base.pill\` — Brass pill
          - \`comet_miner\` — Comet miner
          - \`gw-onboard\` — Onboarding script
          - \`requirements.txt\` — Python dependencies (\`pip install -r requirements.txt\`)

          Usage: \`tar xzf groundwire-<platform>.tar.gz && ./gw-onboard\`
          - \`groundwire-linux-x86_64.tar.gz\`
          - \`groundwire-linux-aarch64.tar.gz\`
          - \`groundwire-macos-aarch64.tar.gz\`"
        env:
          GH_TOKEN: ${{ secrets.GH_PAT }}
